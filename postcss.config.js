/**
 * PostCSS configuration used by Vite to transform CSS. It pulls
 * in Tailwind CSS and Autoprefixer plugins. The default export
 * format is compatible with both CommonJS and ESM in Vite.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};